import React from 'react';

const GalleryMain = ({ imageData }) => {
  return (
    // <div>
    //   {imageData.map((image) => (
    //     <div key={image.id} className="column">
    //       <img src={image.img} alt={`Image ${image.id}`} />
    //     </div>
    //   ))}
    // </div>
   <h1> Hello world</h1>
  );
};

export default GalleryMain;
